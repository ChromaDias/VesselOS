#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd )"

PYBIN="python3"
if [[ -x "$ROOT_DIR/.venv/bin/python3" ]]; then
  PYBIN="$ROOT_DIR/.venv/bin/python3"
fi

exec "$PYBIN" "$ROOT_DIR/scripts/export_wizard.py" "$@"

